# คู่มือใช้งาน iStartSoftFlow (ฉบับภาษาคน)

> English version: [HOW-TO-USE.md](./HOW-TO-USE.md)

## นี่คืออะไร (อธิบายแบบบ้าน ๆ)

ปกติเวลาให้ AI เขียนโค้ด มันมักจะ "เดาแล้วลุย" — ทำเร็วแต่พังบ่อย แก้วน ๆ
เปลืองเงินค่า AI ไปเรื่อย

**iStartSoftFlow คือชุดกฎ + ทีมผู้ช่วย ที่จับ AI มาทำงานแบบมืออาชีพ:**
ถามให้เข้าใจก่อน → วางแผน → **ให้คุณอนุมัติ** → ลงมือทำทีละส่วน → เทสต์ทุกครั้ง
→ ส่งงานพร้อมใบทดสอบ

เปรียบง่าย ๆ: เหมือนเปลี่ยนจาก "จ้างเด็กฝึกงานที่เดางานเก่ง"
เป็น "จ้างทีมที่มีหัวหน้าโปรเจกต์คุมงานให้" — โดยกฎสำคัญ ๆ ถูกบังคับด้วยโปรแกรม
(เรียกว่า hook) ที่ AI **แอบข้ามไม่ได้** ไม่ใช่แค่ขอร้องกันปากเปล่า

ของฟรี (MIT) — คุณจ่ายแค่ค่า AI ที่คุณใช้อยู่แล้ว

---

## ต้องมีอะไรก่อนเริ่ม

1. **Node.js เวอร์ชัน 18 ขึ้นไป** — ตรวจด้วยคำสั่ง `node -v` (ถ้ายังไม่มี โหลดที่ nodejs.org)
2. **Claude Code** — โปรแกรม AI เขียนโค้ดของ Anthropic
   ติดตั้งด้วย `npm install -g @anthropic-ai/claude-code` แล้วล็อกอินตามที่มันถาม
   (ใช้กับ Codex / Cursor / Gemini / Aider ก็ได้ แต่ Claude Code ได้ความสามารถครบสุด)
3. **โฟลเดอร์โปรเจกต์** — จะเป็นงานใหม่ถอดด้าม หรือโปรเจกต์เดิมที่มีโค้ดอยู่แล้วก็ได้

---

## ติดตั้ง (1 นาที)

เปิด Terminal เข้าไปที่โฟลเดอร์โปรเจกต์ แล้วพิมพ์:

```bash
npx create-issflow
```

มันจะถามว่าใช้เครื่องมือ AI ตัวไหน → เลือกของคุณ (ส่วนใหญ่คือ `claude`) จบแค่นั้น

- **ไม่ทับไฟล์เดิมของคุณเด็ดขาด** — ถ้าชนกัน มันเขียนไฟล์ใหม่แยกไว้ให้ดูเอง
- ติดตั้งซ้ำเพื่ออัปเดตได้ตลอด

จากนั้นเปิด Claude Code ในโฟลเดอร์นั้น:

```bash
claude
```

---

## ใช้ยังไง — เลือกตามสถานการณ์ของคุณ

### สถานการณ์ 1: เริ่มโปรเจกต์ใหม่ทั้งอัน

พิมพ์ในหน้าจอ Claude Code:

```
/overview
```

มันจะ**สัมภาษณ์คุณ**เหมือน PM มืออาชีพ: จะสร้างอะไร ให้ใครใช้ มีข้อจำกัดอะไร
ตอบตามจริง ไม่รู้ก็บอกว่าไม่รู้ (มันจะช่วยหาคำตอบให้) สัมภาษณ์จบมันจะเขียน**แผนงาน**
มาให้ดู — **อ่านแล้วกดอนุมัติก่อน มันถึงจะเริ่มสร้างได้** (มีตัวล็อกจริง ๆ
ถ้าแผนยังไม่อนุมัติ มันแก้ไฟล์โค้ดไม่ได้เลย)

อนุมัติแล้วก็สั่งสร้างทีละช่วง:

```
/phase
```

แต่ละช่วงมันจะ เขียนเทสต์ก่อน → เขียนโค้ดให้ผ่านเทสต์ → ตรวจความปลอดภัย → ปิดงาน
เสร็จช่วงไหนก็สั่ง `/phase` ต่อไปเรื่อย ๆ จนจบแผน

### สถานการณ์ 2: เพิ่มฟีเจอร์ให้ระบบที่มีอยู่แล้ว (จุดเด่นสุดของชุดนี้)

**ขั้น 1 — สร้างใบสั่งงาน:**

```
/feature new ชื่อฟีเจอร์
```

มันจะสร้างไฟล์ฟอร์มให้ที่ `docs/features/ชื่อฟีเจอร์/FEATURE.md`

**ขั้น 2 — เขียนอธิบายฟีเจอร์ลงในฟอร์ม** (เขียนภาษาคนธรรมดานี่แหละ):
อยากได้อะไร ทำไม อะไรบ้างที่ต้องทำได้ อะไรที่**ไม่**เอา
เสร็จแล้วแก้บรรทัดบนสุดจาก `Approval: PENDING` เป็น
`Approval: APPROVED ชื่อคุณ วันที่` — นี่คือ "ลายเซ็น" ของคุณ

**ขั้น 3 — ปล่อยงาน:**

```
/feature
```

แล้วไปทำอย่างอื่นได้เลย มันจะ: ตรวจใบสั่งงานหาช่องโหว่ → วางแผนย่อย →
เขียนเทสต์ก่อนแล้วค่อยเขียนโค้ด → ตรวจซ้ำแบบโหด ๆ → ทำ**ใบทดสอบสำหรับมนุษย์**
→ ส่งงานกลับมาเป็น branch

**ขั้น 4 — งานของคุณมีแค่นี้:** เปิดไฟล์ `TEST-PLAN.md` ที่มันทำให้
→ กดทดสอบตามทีละข้อ → ผ่านหมดก็รวมงาน (merge) จบ

### สถานการณ์ 3: แก้งานเล็ก ๆ (แก้ข้อความ เปลี่ยนสี แก้บั๊กจิ๋ว)

```
/quick อธิบายสิ่งที่อยากแก้
```

มันจะ**ทวนความเข้าใจให้คุณยืนยันก่อน** แล้วค่อยแก้ พร้อมรันเทสต์กันของเดิมพัง

---

## คำสั่งทั้งหมด

| พิมพ์ว่า | มันคือ |
|---|---|
| `/overview` | เริ่มโปรเจกต์ใหม่ — สัมภาษณ์ + วางแผน + ขอลายเซ็นคุณ |
| `/feature new ชื่อ` | ขอฟอร์มใบสั่งงานฟีเจอร์ |
| `/feature` | ปล่อยให้มันทำฟีเจอร์จนจบ ส่ง branch + ใบทดสอบกลับมา |
| `/quick ...` | แก้เล็ก ๆ เร็ว ๆ |
| `/phase` | สร้างงานช่วงถัดไปตามแผน |
| `/goal set "..."` | ตั้งเป้าใหญ่ (เช่น "เคลียร์ฟีเจอร์ค้างให้หมด") ให้มันไล่ทำเอง |
| `/goal run` | ให้มันทำงานไล่ตามเป้าจนเสร็จ/หมดโควตาที่ตั้งไว้ |
| `/sprint run` | จัดงานเป็นรอบ ๆ แบบทีม Scrum แล้วขับให้อัตโนมัติ |
| `/propose` | ทำใบเสนอราคาให้ลูกค้า (สำหรับงานรับจ้าง) |
| `/change-request ...` | ลูกค้า/คุณขอเปลี่ยนงานกลางทาง — คิดผลกระทบ+ราคาใหม่ให้โปร่งใส |
| `/release` | ตรวจใหญ่ก่อนขึ้นระบบจริง (เทสต์รวม + ตรวจความปลอดภัย + ให้คนทดสอบ) |
| `/uat` | สร้างชุดทดสอบให้มนุษย์กด + เก็บผล |
| `/ui-audit` `/qa-audit` `/security-audit` | ตรวจสุขภาพทั้งระบบ: หน้าตา / คุณภาพ / ความปลอดภัย |
| `/replan` | แก้แผน (แล้วต้องขอลายเซ็นคุณใหม่) |
| `/synthesize` | สรุปย่องานเก็บไว้ ประหยัดค่า AI รอบต่อไป |
| `/log-issue` `/log-decision` `/runbook` | จดปัญหา / จดการตัดสินใจ / จดวิธีกู้ระบบ |
| `/store-wisdom` | เก็บบทเรียนไปใช้ข้ามโปรเจกต์ |
| `/unstuck` | มันตันแล้ว — สั่งให้ถอยไปหาข้อมูลใหม่แบบจริงจัง |

จำไม่ต้องหมดก็ได้ — จำแค่ 3 ตัวก็ทำงานได้แล้ว: `/overview` เริ่มงานใหม่ ·
`/feature` เพิ่มของ · `/quick` แก้จุกจิก

---

## มันจะ "หยุดถามคุณ" เมื่อไหร่บ้าง

ระบบนี้อัตโนมัติเกือบหมด แต่จงใจเหลือจุดที่**ต้องเป็นคนตัดสินใจ**:

1. **ก่อนเริ่มงานทุกครั้ง** — มันจะเล่าความเข้าใจให้ฟังก่อน คุณยืนยันแล้วค่อยลงมือ
   (กันเข้าใจผิดแล้วทำเสียเวลา+เสียเงิน)
2. **อนุมัติแผน / ใบสั่งฟีเจอร์ / ราคา** — ไม่มีลายเซ็นคุณ = ไม่เริ่ม
3. **ของอันตราย** — ขึ้นระบบจริง, ลบข้อมูล, ส่งอะไรออกนอกบ้าน — หยุดรอคุณเสมอ
4. **ทดสอบรอบสุดท้าย (UAT) + กด merge** — ด่านสุดท้ายเป็นของมนุษย์ตลอด

---

## คำถามที่เจอบ่อย

**ถาม: ไม่เขียนโค้ดเป็น ใช้ได้ไหม?**
ได้ — หน้าที่คุณคืออธิบายว่าอยากได้อะไร + กดทดสอบตามใบที่มันทำให้ + ตัดสินใจอนุมัติ
แต่ถ้าอ่านโค้ดออกจะยิ่งตรวจงานได้ลึก

**ถาม: เสียเงินเท่าไหร่?**
ชุดนี้ฟรี คุณจ่ายแค่ค่าใช้ AI ตามแพ็กเกจที่มีอยู่แล้ว และชุดนี้ถูกออกแบบมา**ประหยัด**:
โหลดเฉพาะที่ใช้ สรุปย่อระหว่างทาง งานถูกส่งให้ผู้ช่วยตัวเล็ก (รุ่นถูกกว่า) เมื่อเหมาะสม

**ถาม: ใช้กับภาษา/เฟรมเวิร์กอะไรได้บ้าง?**
ทุกอัน — มันคุม"วิธีทำงาน" ไม่ได้ผูกกับภาษาไหน คุณประกาศ stack ของคุณครั้งเดียวตอน `/overview`

**ถาม: ถ้า AI ทำพลาดล่ะ?**
มันเขียนเทสต์ก่อนเขียนโค้ดเสมอ พลาดแล้วมีตัวจับ, ลองแก้เกิน 3 ครั้งมันต้องหยุดแล้วรายงาน
(ห้ามมั่วต่อ), ทุกปัญหาถูกจดไว้ไม่ให้พลาดซ้ำ และงานอยู่บน branch แยก — ของจริงไม่พังจนกว่าคุณ merge

**ถาม: อยากให้มันทำงานเองตอนกลางคืน / ตอนไม่อยู่?**
ได้ — ติดตั้งด้วย `npx create-issflow --ci` (ทำงานผ่าน GitHub: ติดป้าย
`feature:approved` ที่ issue แล้วมันทำเอง) หรือ `--docker` (รันในกล่องปลอดภัยบนเครื่องคุณ:
`node scripts/feature-docker.js docs/features/ชื่อ/FEATURE.md`) ต้องมี API key ของ Anthropic

**ถาม: เลิกใช้ยังไง?**
ลบโฟลเดอร์ `.claude/` กับไฟล์ `AGENTS.md` ทิ้ง — โค้ดของคุณไม่ถูกแตะ

---

## อ่านต่อ

- ภาพรวม + ติดตั้งละเอียด: [README.md](./README.md)
- กฎทั้งหมดของระบบ (ฉบับเต็ม): `.claude/istartsoft-flow/METHODOLOGY.md` (หลังติดตั้ง)
- เว็บ: https://iamstarter.github.io/istartsoftflow/
